const dialogRef = document.getElementById('myDialog');

        function openDialog() {
            dialogRef.showModal();
        }
        function closeDialog() {
            dialogRef.close();
        }